# Digital Twin Configuration

# =============================================================================
# Data Paths
# =============================================================================
DATA_DIR=./data
STORAGE_DIR=./storage

# =============================================================================
# Qdrant Configuration
# =============================================================================
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION=digital_twin

# =============================================================================
# GPU Profile (Optional - simplifies configuration based on your hardware)
# =============================================================================
# Set GPU_PROFILE to auto-configure LLM model, TOP_K, and embedding model.
# When set, overrides GPT4ALL_MODEL, TOP_K, and EMBEDDING_MODEL settings.
#
# Available profiles:
#   low    - Integrated GPU / CPU-only (≤4GB VRAM)
#            Model: orca-mini-3b, TOP_K: 3
#   medium - Mid-range GPU like RTX 3060/4060 (6-8GB VRAM)
#            Model: mistral-7b, TOP_K: 5
#   high   - High-end GPU like RTX 3080/4080 (12-16GB VRAM)
#            Model: llama-2-13b, TOP_K: 8
#   ultra  - Enthusiast GPU like RTX 4090/A100 (≥24GB VRAM)
#            Model: nous-hermes-13b-Q5, TOP_K: 12
#
# Leave commented to use manual settings below.
# GPU_PROFILE=medium

# =============================================================================
# Embedding Model (overridden by GPU_PROFILE if set)
# =============================================================================
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# =============================================================================
# LLM Configuration
# =============================================================================
# Provider: gpt4all | openai | anthropic
LLM_PROVIDER=gpt4all

# GPT4All (local) - overridden by GPU_PROFILE if set
GPT4ALL_MODEL=mistral-7b-instruct-v0.1.Q4_0.gguf

# OpenAI (optional - requires API key)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4-turbo

# Anthropic (optional - requires API key)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-sonnet-20240229

# =============================================================================
# RAG Configuration
# =============================================================================
CHUNK_SIZE=1024
CHUNK_OVERLAP=100
# TOP_K is overridden by GPU_PROFILE if set
TOP_K=5

# =============================================================================
# UI Configuration
# =============================================================================
STREAMLIT_PORT=8501
